상단고정 : appBar()
중단고정 : body()
하단고정 : bottomNavigationBar

pubspec.yaml에는 이것저것 다들어감 이미지 추가하고싶으면 flutter 밑에 폴더명/ 해주면
폴더 내부의 모든 파일들이 등록이 됨

```
body: Row(
          children: [
            Icon(Icons.ice_skating),
            Icon(Icons.ice_skating),
            Icon(Icons.ice_skating),
            Icon(Icons.ice_skating),
            Icon(Icons.ice_skating),
          ],
        ),
```
가로로 배치하려면 Row, children
Row()의 mainAxis는 가로축 / crossAxis는 세로축을 정렬
세로는 Column